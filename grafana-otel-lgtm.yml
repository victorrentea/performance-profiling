services:
  grafana-otel:
    # instrument both projects by VM options:
    # -javaagent:/Users/victorrentea/Downloads/docker-otel-lgtm-main/examples/java/opentelemetry-javaagent-v2.1.0.jar -Dotel.instrumentation.micrometer.enabled=true -Dotel.metric.export.interval=500 -Dotel.bsp.schedule.delay=500

    # ⚠️add to SecondApp -Dspring.application.name=second
    # plus the rest as for ProfiledApp -javaagent.....
    image: grafana/otel-lgtm
    ports:
      - "3001:3000"
      - "4317:4317"
      - "4318:4318"
      # login to http://localhost:3000 using admin:admin
